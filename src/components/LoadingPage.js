import LoadingImage from "../assets/loader.gif"

const LoadingPage = () => (
  <div className="loader">
    <img className="loader__image" src={LoadingImage} />
  </div>
)

export default LoadingPage
